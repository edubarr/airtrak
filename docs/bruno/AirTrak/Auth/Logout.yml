info:
  name: Logout
  type: grpc
  seq: 4

grpc:
  url: "{{baseGrpcUrl}}"
  method: /airtrak.auth.v1.AuthService/Logout
  methodType: unary
  protoFilePath: ../../../backend/proto/airtrak/auth/v1/auth.proto
  message: "{}"
  auth:
    type: bearer
    token: "{{accessToken}}"
